import { connect } from "react-redux";
import TeamNameModalScreen from "./TeamNameModalScreen";

const mapStateToProps = ({ teams }) => ({
});

const mapDispatchToProps = (dispatch) => ({
});

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(TeamNameModalScreen);
